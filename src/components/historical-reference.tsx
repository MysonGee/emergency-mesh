import { historicalFloodReference as item } from "@/data/historical-incidents";

export function HistoricalReference() { return <section aria-labelledby="history-title"><p className="eyebrow">Prior incidents & lessons</p><h2 id="history-title">{item.title}</h2><p className="result blocked">{item.currency} · {item.ageLabel}</p><p>{item.extent}</p><p className="notice"><b>Change since source:</b> {item.developmentChange}</p><div className="cards"><article><p>Key points</p><ul>{item.keyPoints.map(x => <li key={x}>{x}</li>)}</ul></article><article><p>Check this time</p><ul>{item.checks.map(x => <li key={x}>{x}</li>)}</ul></article></div><p><b>Advisory:</b> {item.recommendation}</p><small>Source: {item.source} · Last validated: {item.lastValidated} · Historical reference only; human review required.</small></section>; }
